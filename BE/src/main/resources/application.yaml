spring:
  output:
    ansi:
      enabled: always

  application:
    name: online_learning

  datasource:
    url: jdbc:mysql://mainline.proxy.rlwy.net:50911/railway?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: CfdTcHtJtyVSUMUUdFrLETTtuIggNMDu
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        # dialect: org.hibernate.dialect.MySQLDialect (OPTIONAL)

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.tool.schema: DEBUG
    org.springframework.security: DEBUG


# =======================
# GEMINI AI CONFIG
# =======================
gemini:
  api-key: ${GEMINI_API_KEY}
  model: gemini-flash-latest
  base-url: https://generativelanguage.googleapis.com/v1beta/models/

  timeout: 30s
  max-tokens: 2048
  temperature: 0.7

# =======================
# AI PROMPT VERSIONING (optional nhưng rất nên có)
# =======================
ai:
  prompt:
    exercise-version: v1.0
    assistant-version: v1.0
